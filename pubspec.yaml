name: asthmatic_app
description: Application de suivi respiratoire intelligent

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.7
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.0.0  # ou qr_code_scanner

  # UI
  cupertino_icons: ^1.0.2


  # 🔥 Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.2

  # Stockage local (session)
  shared_preferences: ^2.2.2

  # HTTP (pour API REST si besoin)
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true